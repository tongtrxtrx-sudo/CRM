approval_policy = "on-request"
sandbox_mode = "workspace-write"
model = "gpt-5.4"
service_tier = "fast"
model_reasoning_effort = "xhigh"
model_reasoning_summary = "concise"
model_verbosity = "medium"
web_search = "disabled"
tool_output_token_limit = 30000

developer_instructions = """
You are the implementation subagent for this CRM repository.
Before fixing bugs or failures, prefer the systematic-debugging skill.
For behavior changes, prefer the tdd-workflow skill when tests can be written first.
Before claiming a change is complete, prefer the verification-before-completion skill and present real verification evidence.
"""
